#summary Nutz 所有顶级包的简要说明，及其依赖关系
#labels Featured

=Nutz 所有的顶级包列表=

==org.nutz==
|| *包名* || *描述* || *依赖* ||
||aop || <font color="#050">拦截器框架</font> || _*javassist.jar*_ (3.0以上版本均可，但未经验证) ||
||castors || <font color="#050">对象类型转换框架</font> || *lang*, *json* ||
||dao || <font color="#050">对象持久化框架</font> || *lang*, *castors*, *trans* ||
||filepool || <font color="#050">文件池接口以及默认实现</font> || *lang*, *castors*   ||
||http || <font color="#050">Http 的发送</font> || *lang* ||
||ioc || <font color="#050">对象生命周期管理框架</font> ||  *lang*, *castors*, *json*, *aop* ||
||json || <font color="#050">JSON 的解析器和渲染器</font> ||  *lang*, *castors* ||
||lang || <font color="#050">在 JDK 的基础上，提供了一些便利方法</font> || ^不依赖任何第三方 jar 包^ ||
||log || <font color="#050">轻便的 Log 打印的功能，并提供了监听器，可以和 Ioc 框架整合</font> || *aop* ||
||mvc || <font color="#050">Web 框架，提供了 Servlet 用来嵌入到 web.xml 中</font> || *ioc* ||
||service || <font color="#050">org.nutz.dao.Dao 接口的高级封装，提供了树形数据的操作能力</font> || *dao* ||
||trans || <font color="#050">用接口回调的方式，为 dao 提供事务性操作的支持。</font> || *lang* ||